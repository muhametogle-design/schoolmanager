# Frontend environment template.
# Copy to `.env.local` and adjust as needed; Vite only exposes VITE_* variables to the client.
#
# Absolute URL of the FastAPI backend (must include the /api/v1 prefix).
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
